When project updates occur

- Check if README.md needs updating after any significant changes
- Update feature list if new features are added
- Update page list if new pages are created
- Update technology stack if new libraries are added
- Update color scheme if theme colors change
- Keep README concise and relevant